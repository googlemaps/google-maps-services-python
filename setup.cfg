[tool:pytest]
addopts = -rsxX
testpaths = tests

[coverage:run]
source = googlemaps
omit =
    tests/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise NotImplementedError